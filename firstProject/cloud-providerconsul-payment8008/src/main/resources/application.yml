server:
  # consul服务端口
  port: 8008
spring:
  application:
    name: cloud-provider-payment
  cloud:
    consul:
      # consul注册中心地址
      host: 47.105.192.45
      port: 8600
      discovery:
        hostname: 127.0.0.1
        service-name: ${spring.application.name}